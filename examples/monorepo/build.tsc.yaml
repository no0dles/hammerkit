tasks:
  build:
    image: node:24-alpine
    deps: [ npm:install ]
    src:
      - tsconfig.json
      - src
    cmds:
      - node_modules/.bin/tsc -b
    generates:
      - dist

includes:
  npm: ./build.npm.yaml
