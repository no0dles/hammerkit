tasks:
  install:
    description: install dependencies
    image: node:14.16.0
    cmds:
      - npm install
    generates:
      - node_modules
    mounts:
      - $PWD/.npm:/.npm
      - $PWD/.config:/.config
    src:
      - package.json
      - package-lock.json
