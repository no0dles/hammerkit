envs:
  NODE_VERSION: 16.6.0

tasks:
  prettier:
    description: run prettier
    deps: [npm:ci]
    image: node:$NODE_VERSION-alpine
    src:
      - src
      - .prettierrc
    generates:
      - dist
    cmds:
      - node_modules/.bin/tsc

includes:
  npm: build.npm.yaml
