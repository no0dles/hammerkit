apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres
          env:
            - name: POSTGRES_USER
              value: api
            - name: POSTGRES_DB
              value: api
            - name: POSTGRES_PASSWORD
              value: "123456"
          ports:
            - containerPort: 5432
