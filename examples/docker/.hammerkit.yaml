tasks:
  example:
    description: docker example
    # pinned patch: docker.spec.ts asserts the exact npm/node versions this image prints
    image: node:24.16.0-alpine
    cmds:
      - npm -v
      - node -v
